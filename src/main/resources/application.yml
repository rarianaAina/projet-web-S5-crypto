spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cryptotrading
    username: postgres
    password: rariana
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8090